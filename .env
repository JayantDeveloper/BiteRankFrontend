# API URL - Update this when deploying to production
VITE_API_URL=http://localhost:8000/api

# Admin credentials (never commit real values to git)
VITE_ADMIN_USER=jaym
VITE_ADMIN_PASS=tintin10!

# Scrape API key — must match SCRAPE_API_KEY on the backend
VITE_SCRAPE_API_KEY=
