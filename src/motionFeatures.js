// Lazily-loaded Framer Motion feature bundle (layout + gestures + exit).
// Kept in its own chunk so the main bundle stays lean; LazyMotion picks it up.
export { domMax as default } from 'framer-motion'
